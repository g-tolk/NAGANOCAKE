<h3>ジャンル一覧・追加</h3>

<table class="table table-hover">
    <thead>
      <tr>
        <th>ジャンル</th>
        <th>状態</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @genres.each do |genre| %>
    	　<tr>
          <td></td>
          <td></td>
          <td><%= link_to "編集する", admins_genres_edit_path(@genre),class:"btn btn-primary" %></td>
        </tr>
      <% end %>
    </tbody>
</table>